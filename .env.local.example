# Backend API URL — update after deploying the backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# API key — must match the API_KEY set in the backend's .env
# Used to authenticate write operations (create incident, resolve, reset)
NEXT_PUBLIC_API_KEY=sentinel-dev-key
